spring.application.name=email-writer-sb

# Keep only the endpoint path here, not the full URL
gemini.api.url=/v1beta/models/gemini-2.0-flash:generateContent

# Just the key string, don't add ?key= here
gemini.api.key=
